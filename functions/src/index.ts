const charge = require('./charge');
const pushorder = require('./pushorder');
const thumbnail = require('./thumbnail');
const processorder = require('./processorder');
const contactus = require('./contactus');

module.exports = {
    charge,
    pushorder,
    thumbnail,
    processorder,
    contactus
};

